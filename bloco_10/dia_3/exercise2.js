const returnToUpperCase = (string) => string.toUpperCase();

const returnFirstCharacter = (string) => string[0];

const concatStrings = (string1, string2) => string1.concat(string2);

module.exports = { returnToUpperCase, returnFirstCharacter, concatStrings };
